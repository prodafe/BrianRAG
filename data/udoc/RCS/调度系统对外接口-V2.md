---
title: 调度系统对外接口-V2
description: 
published: true
date: 2025-12-17T11:50:31.692Z
tags: 公开, 协议, 接口, rcs
editor: markdown
dateCreated: 2025-07-09T06:51:50.153Z
---

## 1.任务创建接口

### 1.1 双目标点创建接口

**接口地址**:`Ip:8092/api/AgvTask/CreatePairJob`

**请求方式**:`POST`

**请求数据类型**:`application/json`

**响应数据类型**:`application/json`

**说明**:  创建任务的时候同时告知起点和终点，起点接料，终点送料

**请求示例:**

```json
{
    "id": "10001",
    "start": "D001",
    "end": "D002",
    "extraInfo": {
        "quality": "Bad"
    },
    "callTime": "2025-02-10 10:00:00",
    "source": 1,
    "remark": ""
}
```

**请求参数**:&#x20;

**正确响应示例**:

```json
{
    "status": 200,
    "isSuccess": true,
    "message": "操作成功",
    "data": 1
}
```

**错误响应示例**:

```json
{
    "status": 500,
    "isSuccess": false,
    "message": "{错误信息}",
    "data": null
}
```

**响应参数**:

### 1.2 单目标点创建任务

**接口地址**: `ip:8092/api/AgvTask/CreateSingleJob`

**请求方式**:`POST`

**请求数据类型**:`application/json`

**响应数据类型**:`application/json`

**请求示例:**

```json
{
    "id": "10002",
    "target": "D001",
    "taskType": 1,
    "extraInfo": {
        "quality": "Bad"
    },
    "callTime": "2025-02-10 10:00:00",
    "isAllowRepeat": false,
    "source": 1,
    "remark": ""
}
```

**请求参数**:&#x20;

**正确响应示例**:

```json
{
    "status": 200,
    "isSuccess": true,
    "message": "操作成功",
    "data": 1
}
```

**错误响应示例**:

```json
{
    "status": 500,
    "isSuccess": false,
    "message": "{错误信息}",
    "data": null
}
```

**响应参数**:

### 1.3 设备呼叫创建多个单点任务

**接口地址**:`ip:8092/api/AgvTask/CreateMultiSingleJob`

**请求方式**:`POST`

**请求数据类型**:`application/json`

**响应数据类型**:`application/json`

**请求示例: 注意是个数组，主要是给IoT调用的，同时响应多个呼叫**

```json
[
    {
        "id": "10002",
        "target": "D001",
        "taskType": 1,
        "extraInfo": {
            "quality": "Bad"
        },
        "callTime": "2025-02-10 10:00:00",
        "isAllowRepeat": false,
        "remark": "",
        "source": 1
    }
]
```

**请求参数**:&#x20;

**正确响应示例**:

```json
{
    "status": 200,
    "isSuccess": true,
    "message": "操作成功",
    "data": 1
}
```

**错误响应示例**:

```json
{
    "status": 500,
    "isSuccess": false,
    "message": "{错误信息}",
    "data": null
}
```

**响应参数**:

## 2.料口信息获取

### 2.1 料口信息获取

**接口地址**:` ip:8090/api/TransferPort/GetTransferInfos`

**请求方式**: `Get`

**请求数据类型**:`application/json`

**响应数据类型**:`application/json`

**请求参数**:`无`

**正确响应示例**:

```json
{
  "status": 200,
  "isSuccess": true,
  "message": "操作成功",
  "data": [
    {
      "id": "T001",
      "name": "料口1"
    },
    {
      "id": "T002",
      "name": "料口2"
    }
  ]
}
```

**错误响应示例**:

```json
{
    "status": 500,
    "isSuccess": false,
    "message": "{错误信息}",
    "data": null
}
```

**响应参数**:

### 2.2 库位信息获取

**接口地址**:`ip:8093/api/Storage/GetStorageInfos`

**请求方式**: `Get`

**请求数据类型**:`application/json`

**响应数据类型**:`application/json`

**请求参数**:`  无`

**正确响应示例**:

```json
{
  "status": 200,
  "isSuccess": true,
  "message": "操作成功",
  "data": [
    {
      "id": "T001",
      "name": "料口1"
    },
    {
      "id": "T002",
      "name": "料口2"
    }
  ]
}
```

**错误响应示例**:

```json
{
    "status": 500,
    "isSuccess": false,
    "message": "{错误信息}",
    "data": null
}
```

**响应参数**:

### 2.3 库位更新

**接口地址**: `ip:8093/api/Storage/UpdateStorageInfoByPDA?storageId=11&currentNumber=11`

**请求方式**: `Get`

**请求数据类型**:`application/json`

**响应数据类型**:`application/json`

**请求参数**:&#x20;

**正确响应示例**:

```json
{
  "status": 200,
  "isSuccess": true,
  "message": "操作成功",
  "data": [
    {
      "id": "T001",
      "name": "料口1"
    },
    {
      "id": "T002",
      "name": "料口2"
    }
  ]
}
```

**错误响应示例**:

```json
{
    "status": 500,
    "isSuccess": false,
    "message": "{错误信息}",
    "data": null
}
```

**响应参数**:

## 3.任务取消

### 3.1 通过任务JobId取消任务



**接口地址**`ip:8093/api/Job/cancelJob/{id}`

**请求方式**: `post`

**请求数据类型**:`application/json`

**响应数据类型**:`application/json`

**请求参数**: `id: 任务jobId`

**正确响应示例**:

```json
{
  "status": 200,
  "isSuccess": true,
  "message": "操作成功",
  "data": {}
}
```

**错误响应示例**:

```json
{
    "status": 500,
    "isSuccess": false,
    "message": "任务已经执行无法取消"    //错误信息, 不固定,
    "data": null
}
```

**响应参数**:

### 3.2 通过任务目标点取消任务



**接口地址**`ip:8093/api/Job/cancel/{target}`

**请求方式**: `post`

**请求数据类型**:`application/json`

**响应数据类型**:`application/json`

**请求参数**:` targert: 任务目标点`

**正确响应示例**:

```json
{
  "status": 200,
  "isSuccess": true,
  "message": "操作成功",
  "data": {}
    
}
```

**错误响应示例**:

```json
{
    "status": 500,
    "isSuccess": false,
    "message": "任务已经执行无法取消"    //错误信息, 不固定,
    "data": null
}
```

**响应参数**:

